# Build log

- 2026-07-22: repo created, scoping the 402 gateway + ZeroClaw config.
- 2026-07-22: reframed as an operator toolkit — any machine, any service (GPU/storage/bandwidth), one services.toml + adapter scripts. Reference deployment stays GPU + storage on my own rigs.
- 2026-07-23: gateway v0.1 built and smoke-tested — x402 flow (402 quote → Solana Pay URL → live mainnet RPC verification), adapter runner with timeouts, jsonl ledger, shaped /report/today. 9 unit tests, zero keys held.
- 2026-07-23: gateway v0.2 — capacity hardening (bounded quotes + eviction, per-service concurrency, RPC retry/timeouts, tracing, graceful shutdown, config validation) and the mock-RPC paid-path integration test. 16/16 tests, clippy clean. Standards codified in CLAUDE.md.
- 2026-07-23: authored the full ZeroClaw agent composition against real master schema (cloned zeroclaw, extracted v3 config/skill/SOP formats): Telegram config, operator skill, daily-reconciliation + refund-review SOPs with human checkpoint, AGENTS.md injection posture, install guide.
- 2026-07-23: renamed the project rigpay → rende ('o dinheiro rende' — the money yields). GitHub repo, crate, configs, skills, SOPs all swept; 16/16 tests green under the new name.
- 2026-07-23: installed ZeroClaw v0.8.3 (stock prebuilt), deployed config/skills/SOPs to ~/.zeroclaw. Both SOPs pass 'zeroclaw sop validate'; refund-review checkpoint parses as [confirmation required] in the real binary. Gotcha found: sops_dir is cwd-relative — documented, use absolute path.
- 2026-07-23: agent brain is now fully self-hosted — Ollama on the RTX 5080 rig over Tailscale (OLLAMA_HOST override + qwen2.5:7b-instruct for native tool calling). Telegram bot @rendepay_bot created and token wired locally.
- 2026-07-27: brought the whole stack live for the demo. Diagnosed the old 1200s-timeout/phantom-denial: qwen2.5:7b calls ambient tools (ask_user, channel_room) instead of http_request — fixed by pinning allowed_tools to http_request+memory and excluding ask_user. Agent now answers "how's business?" from the real /report/today in ~7s. Skill hardened: never state prices from memory (fixed a 50-USDC hallucination). Real receive address wired, demo/quote_qr.sh renders the Solana Pay QR for the phone take. Injection probe: agent refuses to execute the refund and routes to the SOP checkpoint. Pulling qwen3:14b for reliable quote issuance.
- 2026-07-27 (later): found the real pay_url-fabrication culprit — ZeroClaw's http_request returns only status text on non-2xx, so the x402 402 body (with pay_url/job_id) never reached the model; every model "helpfully" invented one. Added GET/POST /quote/{service_id} (same quote, HTTP 200) + integration test (17/17). Upgraded brain to qwen3:14b @ temp 0.2. Customer quote flow now 3/3 with real Solana Pay URLs; chat-native paid flow (agent resubmits X-Job-Id); injection probe refused verbatim and flagged for oversight.
